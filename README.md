# TheFactoryTest

The target repository for [TheFactory](https://github.com/ldm314/TheFactory): an
autonomous software factory whose requirement graph is the source code. The
factory owns everything on `main` from here forward — components, migrations,
flags, SBOM — and every change lands through a factory release.

Earlier factory runs live in this repository's history.
