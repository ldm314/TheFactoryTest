# Running Factory Test 4

Generated by The Factory. This file, `app.py`, `flags.json`, the `Dockerfile`
and `docker-compose.yml` are regenerated whenever the application gains or
changes a component — edits to them are lost.

## With a database

```
docker compose up --build
curl localhost:8080/health
```

Something already on 8080? `APP_PORT=9000 docker compose up --build`.

## Without

```
DATABASE_URL=postgresql://... python app.py
```

Every component of this application answers on one port. `GET /health` lists
them and reports which flags are on.

## Components

- `foundation/__init__.py` serves `/foundation` — the system shall provide a runnable application entry point with a health check and persistence configured for the selected application scaffold
- `oauth/__init__.py` serves `/oauth` — Expected: Keep token path and type as code constants; suppress static security warnings (Simplest implementation; accepts advisory warning as acceptable for non-sensitive constant metadata); Other adjustments or clarifications: CRITICAL package isolation: implement POST /oauth/token only in oauth_token/__init__.py (SERVICE_NAME=oauth_token). Do not modify or replace oauth/__init__.py — GET /oauth/authorize from W-1259 must keep working. Do not invent GET /oauth/health or any /oauth/health handler. TOKEN_PATH and TOKEN_TYPE may stay as plain code constants. Currently: Checks that failed: security.static: warning (advisory, not blocking): oauth2_authorization_code.py:8:14: S105 Possible hardcoded password a — repair stalled after 1 cycle(s): the same checks failed identically
- `oauth_clients/__init__.py` serves `/oauth/clients` — the system shall register OAuth clients with a client_id and client_secret and refuse unknown clients
- `runnables/__init__.py` serves `/runnables` — the system shall accept and store runnable configuration via POST /runnables
- `users/__init__.py` serves `/users` — Expected: Email must be unique across all owners (Any user with a given email is rejected system-wide, regardless of owner. Duplicate detection happens on first attempt.); Reject empty or whitespace-only emails with HTTP 400 Bad Request (Input validation prevents storage of invalid email values before uniqueness checks occur.); Always return 409 Conflict on duplicate email (Each duplicate attempt fails. No idempotent behavior; caller must handle retry logic.); Any other clarification or alternative approach?: No product change. Factory bug fixed: property checks and duplicate-email criteria no longer collide on shared UNIQUE_FIELDS values across tests. Resynthesize and rebuild with the original statement (email unique key, 409 on duplicate, return by id). Currently: Behaviours that did not hold: Scenario: Attempt to store a user with a duplicate email — repair stalled after 1 cycle(s): the same checks failed identically

## Flags

A component is **dark** from the moment its code merges until an engineer
accepts the requirement and promotes its flag (docs/09 D-26). A route behind a
flag that is off answers `404`: the endpoint does not exist as far as a caller
can tell. `flags.json` is the state, and it is written by a release — one commit
per promotion, carrying the work item that caused it.

Currently on:

_none: everything is dark_
